apiVersion: v1
kind: Service
metadata:
  name: {{ include "hlf-rest-client.fullname" . }}
  labels:
    {{- include "hlf-rest-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hlf-rest-client.selectorLabels" . | nindent 4 }}
